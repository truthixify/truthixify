# Journal sync (server-side only — never exposed to the browser)
GITHUB_TOKEN=        # Optional: github.com → Settings → Developer settings → Personal access tokens (classic) → public_repo scope only
REVALIDATE_SECRET=   # Any random string; must match the GitHub webhook secret
